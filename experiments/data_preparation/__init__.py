"""Dataset preparation notebooks and shared code under ``src/``."""

# Notebooks import from ``experiments.data_preparation.src``; keep this package
# lightweight so ``configure_notebook_environment()`` does not load audio/TTS deps.
